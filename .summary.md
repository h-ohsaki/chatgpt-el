# Handover Notes

## Repository Overview

- Project: `chatgpt-el`
- Main entry points:
  - `chatgpt-api`: Python CLI for ChatGPT, Gemini, and Llama-compatible API backends.
  - `chatgpt.el`: Emacs Lisp front-end that calls the CLI or browser-based workflows.
- Working tree state at handover:
  - `chatgpt-api` modified
  - `chatgpt.el` modified
  - `chatgpt-api-img` untracked
  - `Makefile` untracked
  - `.obsolete/` untracked
  - `__pycache__/` untracked

## Current Change Theme

- The API client is being repointed from Ollama to a Llama/OpenAI-compatible endpoint.
- The Emacs integration now defaults the API engine to `llama`.
- The `llama` model list currently resolves to a placeholder `default` entry.

## Important Files

- `chatgpt-api`
  - Introduces `Llama` as the API backend class.
  - Sends OpenAI-style chat completions payloads to `LLAMA_URL`.
  - `ChatGPT` and `Gemini` now inherit from `Llama`.
  - The CLI default engine is `llama`.
- `chatgpt.el`
  - Default API engine changed to `llama`.
  - Model mapping updated from `ollama` to `llama`.

## Constraints And Notes

- Do not assume untracked files are part of the desired commit without checking.
- Avoid committing cache or obsolete artifacts unless explicitly needed.
- No tests have been run yet for the current change set.

## Open Questions

- Should `Makefile` be committed as part of this change?
- Should `chatgpt-api-img` be committed as part of this change?
- Is the `llama` model list placeholder intentional, or should it be replaced with real model names?
